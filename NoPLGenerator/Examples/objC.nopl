
#"Testing schema objects";

Object schema = schema;
Object elements = schema.elements;
int count = elements.count;

for(int i = 0; i < count; i++)
{
	Object element = elements[i];
	#element->name;
}
