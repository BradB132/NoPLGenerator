
console("generating c++ implementations");

Object classes = abstractions.classes;

for(int i = 0; i < classes.count; i++)
{
	Object class = classes[i];

	outputToFile("./c++_output/"+class.className+".cpp");
}